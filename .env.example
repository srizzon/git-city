# Git City - Environment Variables
# Copy this file to .env.local and fill in your values
# Git City - 环境变量
# 请根据您的项目配置填写以下环境变量

# App Config
# 应用配置
NEXT_PUBLIC_BASE_URL=http://localhost:3001

# Supabase Config
# Supabase 配置
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# GitHub API Config (personal access token for API calls)
# GitHub API 配置 (用于 API 调用的个人访问令牌)
GITHUB_TOKEN=ghp_your-github-token

# Stripe Config (optional - only needed for payments)
# Stripe 配置 (可选 - 仅在需要支付时才需要)
STRIPE_SECRET_KEY=sk_test_your-stripe-key
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret

# AbacatePay Config (optional - BRL payments)
# AbacatePay 配置 (可选 - 仅在需要 BRL 支付时才需要)
ABACATEPAY_API_KEY=abc_dev_your-key
ABACATEPAY_WEBHOOK_SECRET=your-webhook-secret

# Himetrica Config (optional - analytics)
# Himetrica 配置 (可选 - 仅在需要分析时才需要)
NEXT_PUBLIC_HIMETRICA_API_KEY=hm_your-api-key

# Resend Config (optional - ad expiry emails)
# Resend 配置 (可选 - 仅在需要发送广告过期邮件时才需要)
RESEND_API_KEY=re_your-resend-key

# Vercel Cron secret (for scheduled tasks)
# Vercel 计划任务密钥 (用于计划任务)
CRON_SECRET=your-cron-secret
